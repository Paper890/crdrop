BOT BY SAN
<pre><code>sudo apt update -y
sudo apt upgrade -y
sudo apt install python3 python3-pip
pip install requests python-telegram-bot schedule</code></pre>

<h1>Clone Repo</h1>
<pre><code>git clone https://github.com/Paper890/crdrop.git</code></pre>
